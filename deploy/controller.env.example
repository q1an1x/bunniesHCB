# Copy to /etc/bunnieshcb/controller.env and replace this placeholder.
HCB_KNX_GATEWAY=knxd.example.invalid
# Optional in LIVE mode; use a token file readable only by root and the service user.
# HCB_HA_HOST=homeassistant.example.invalid:8123
# HCB_HA_TOKEN_FILE=/etc/bunnieshcb/ha-token
